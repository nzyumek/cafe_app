// modalを閉じる処理
$("#cafe_listModal").modal("hide");
$("body").removeClass("modal-open");
$(".modal-backdrop").remove();

// 画面の書き換え
$('[data-model_name="cafe_list"]').filter('[data-id="<%= @cafe_list.id %>"]').remove();