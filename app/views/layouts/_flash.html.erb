<div id="flash" class="mt-5">
  <% flash.each do |key, value| %>
    <div class="alert <%= bs4_bgcolor_for(key) %> alert-dismissable fade show">
      <%= value %>
      <button class="close" data-dismiss="alert">x</button>
    </div>
  <% end %>
</div>